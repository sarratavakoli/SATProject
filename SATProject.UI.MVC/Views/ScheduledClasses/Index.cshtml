@model IEnumerable<SATProject.DATA.EF.Models.ScheduledClass>

@{
    ViewData["Title"] = "Scheduled Classes";
}


@section css {
<!-- Custom styles for lists template -->
<link href="~/css/style.css" rel="stylesheet">
<link href="~/css/style-responsive.css" rel="stylesheet">
<link rel="stylesheet" href="~/css/to-do.css">
}

<section class="wrapper">
    <!-- COMPLEX TO DO LIST -->
    <div class="row mt">
        <div class="col-md-12">
            <section class="task-panel tasks-widget">
                <div class="panel-heading">
                    <div class="pull-left">
                        <h5> @ViewData["Title"]</h5>
                    </div>
                    <br>
                </div>
                <div class="panel-body">
                    <div class="task-content">
                        <ul class="task-list">

                            @foreach (var item in Model)
                            {
                                <li>
                                    <div class="task-title">
                                        <a asp-action="Details" asp-route-id="@item.ScheduledClassId">@Html.DisplayFor(modelItem => item.Course.CourseName) - @Html.DisplayFor(modelItem => item.InstructorName)</a>                                        
                                        <div class="pull-right">
                                            <a class="btn btn-primary btn-xs" asp-action="Edit" asp-route-id="@item.ScheduledClassId"><i class="fa fa-pencil"></i></a>
                                            <a class="btn btn-success btn-xs" asp-action="Details" asp-route-id="@item.ScheduledClassId"><i class="fa fa-eye"></i></a>
                                            @if (User.IsInRole("Admin"))
                                            {
                                                <a class="btn btn-danger btn-xs" asp-action="Delete" asp-route-id="@item.ScheduledClassId"><i class="fa fa-trash"></i></a>
                                            }
                                        </div>
                                    </div>
                                </li>
                            }
                        </ul>
                    </div>
                    <div class=" add-task-row">
                        <a class="btn btn-success btn-sm pull-left" asp-action="Create">Add Class</a>
                    </div>
                </div>
            </section>
        </div>
        <!-- /col-md-12-->
    </div>
    <!-- end to do list-->
</section>

@section Scripts {
<script src="http://code.jquery.com/ui/1.10.3/jquery-ui.js"></script>
<script src="lib/tasks.js" type="text/javascript"></script>
<script>
    jQuery(document).ready(function() {
      TaskList.initTaskWidget();
    });

    $(function() {
      $("#sortable").sortable();
      $("#sortable").disableSelection();
    });
</script>
}









@*
<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InstructorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ss)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InstructorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ss.Scsname)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ScheduledClassId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ScheduledClassId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ScheduledClassId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
*@